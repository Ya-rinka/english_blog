<!DOCTYPE html>
<html>
  <head>
    <title>EnglishBlog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    
    <nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark">
      <div class="container">
        <span><%= link_to "Home page", home_path, class: "text-light lead" %></span>
        <span><%= link_to "About", about_path, class: "text-light lead" %></span>
        <div class="col-4 text-center">
          <h3><strong class="blog-header-logo text-light">TeacherED</strong></h3>
        </div>
        <% if user_signed_in? %>
          <span><%= link_to "Add article", new_teacher_path, class: "text-light lead" %></span> 
          <span><%= link_to "Log out", destroy_user_session_path, method: :delete, 
            class: "btn btn-sm btn-outline-secondary text-light lead" %></span>
        <% else %>
          <span><%= link_to "Log in", new_user_session_path, class: "text-light lead" %></span>
          <span><%= link_to "Sign up", new_user_registration_path, class: "btn btn-sm btn-outline-secondary text-light lead" %></span>
        <% end %>
      </div>
    </nav>
    

    <div class="container main">
      <% flash.each do |name, message| %>
        <div class="alert <%= bootstrap_class_for(name) %>">
          <button class="close" data-dismiss="alert">&times;</button>
          <%= message %>
        </div>
      <% end %>
      <div class="container">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
